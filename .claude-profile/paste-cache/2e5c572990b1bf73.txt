I have some doubts in /home/durgesh/aditya/C-AIMMS/.claude-profile/plans/c-aimms-adastore-implementation-splendid-cherry.md. Doubts: 1. that COLM’s ADASTORE is meant to be read through FluxMem’s appendices wherever COLM is terse. Explain what this means. In step 8, why is c(e) ≥ τ_c confidence gate from FluxMem Eq. 6, “disabled by default” since COLM omits it. Why does it omit it? would that cause an issue with the proposal we have. Wherever there is discrepancy follow the original paper. Explain the difference between 1-r(e) ≥ τ_u and 1-u(m) ≥ τ_u and what would be the changes from this. 1-r_c(e_j) (the standalone recency) and (e_j) (the recency term inside u) are both recency. Implement “r” as raw normalized recency and “d” as the decayed term; document that under the “utility” reading recency is counted twice, once softly and once as a hard gate. Separately: COLM’s “e_j” “access frequency” inside “U” is “not” FluxMem’s “c(m)” = “confidence” in the optional gate — same letter, different quantity. Name them “access_frequency” and “confidence” so the collision cannot occur in the code. Explain this part in more detail. I understand the second ambiguity. Why is not a real NER: a regex/capitalization heuristic NER, **flagged in the docstring as an approximation, not real NER**? Explain what this means: **true LRU on last-access time** (FluxMem Eq. 5 **not** needed) since we implement LRU and that “ent” and lates the two; document that we implement LRU and that the “oldest” is jumped to bottom (ctrl+End) and later the two; document that we implement LRU and that the “oldest” is jumped to bottom (ctrl+End) and later the two.