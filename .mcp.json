{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/home/durgesh/aditya/C-AIMMS"
      ],
      "description": "Read/write project files, datasets, and experiment records."
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      },
      "description": "Issues, PRs, branches, releases. Needs a PAT with repo scope, exported in your shell."
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Knowledge graph for research findings and entity relationships across sessions."
    },
    "serena": {
      "command": "serena",
      "args": [
        "start-mcp-server",
        "--context",
        "claude-code",
        "--project",
        "/home/durgesh/aditya/C-AIMMS"
      ],
      "description": "LSP-backed symbol search and safe symbol-level edits (rename/move/inline) across Python. Install: uv tool install -p 3.13 serena-agent"
    },
    "headroom": {
      "command": "headroom",
      "args": ["mcp", "serve"],
      "env": {
        "HEADROOM_PROXY_URL": "http://127.0.0.1:8787"
      },
      "description": "Context optimization layer - compresses large tool outputs, provides on-demand retrieval via headroom_retrieve tool."
    },
    "omniroute": {
      "type": "http",
      "url": "http://localhost:20128/api/mcp/stream",
      "description": "Local AI gateway/router (160+ providers, token compression, auto-fallback). Start with: omniroute. Dashboard: http://localhost:20128"
    },
    "whatsapp": {
      "command": "bun",
      "args": [
        "run",
        "--cwd",
        "${CLAUDE_PLUGIN_ROOT}",
        "--shell=bun",
        "--silent",
        "start"
      ],
      "description": "WhatsApp messaging channel via Baileys protocol. Configure with /whatsapp-claude-channel:configure"
    }
  }
}
